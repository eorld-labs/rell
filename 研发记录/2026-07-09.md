# 研发记录：2026-07-09

## 研发主题

RELL 样品从 0 到 1 进入阶段零，建立 P016 Runtime Core 的 Schema、Adapter 合约、Mock 时间轴和校验门禁。

## 参与人/执行主体

- 项目负责人：用户
- 工程协作：Codex

## 今日目标

1. 将“倒水”样品收敛为单过程实例，不进入多动作过程链。
2. 固定 P016 Runtime Core 第一阶段的数据结构边界。
3. 固定 Robot Adapter 的异步接口和最小能力词汇表。
4. 固定 MockRobotAdapter 的时间轴剧本方式，避免第一阶段陷入真实视觉、真实流体或机器人 SDK 联调。
5. 建立阶段零校验脚本，形成后续开发的最低门禁。

## 对应技术层

- [x] 真实世界经验引擎层
- [x] 物理动作过程模板层
- [x] 偶然绑定与过程实例层
- [x] 连续状态变量与阶段跃迁层
- [x] 双通道因果事实验真层
- [x] 最终成立状态与恢复/人工确认层
- [ ] 多主体空间治理层

## 对应专利/技术链特征

1. P016：过程模板定义物理动作的不变过程结构，样品中体现为 `align -> tilting -> maintain_flow -> return` 四阶段倒水过程。
2. P016：偶然绑定数据将当前环境对象、传感器和阈值填充为运行实例，样品中体现为 `pour_water_process_instance.json` 的 `bound_parameters`。
3. P016：Runtime 基于连续状态变量观测值与绑定阈值判断阶段跃迁，Adapter 不直接判断跃迁条件。
4. P016：目标因果事实通过至少两个独立观测通道形成成立判断，样品中体现为 `physical_liquid_level` 与 `digital_flow_integral`。
5. P016：双通道冲突时不做自动融合，降级进入人工确认，防止系统以单一通道继续执行。
6. P016：失败和未知状态受超时、确认窗口、最大恢复次数和人工升级边界约束。
7. RELL：无经验、无绑定、无能力或无观测通道时输出“不会做”，不进入猜测式执行。

## 输入材料

1. `技术库/RELL样品落地0到1计划.md`
2. `技术库/RELL样品开发执行计划.md`
3. 团队对异步状态机、Schema 冻结、双通道验真、Mock 剧本、人工确认、恢复边界和 API 收口的审查意见。
4. P016 已提交方案中的过程模板、偶然绑定、连续状态变量、阶段跃迁、目标因果事实、最终成立状态等技术表达。

## 研发过程

1. 建立阶段零样品目录 `demo_runtime/rell_sample/`，将其定位为 Schema、接口和 Mock 数据冻结层，不实现完整 Runtime。
2. 新增 Runtime 最小数据结构 Schema，包括运行事件、阶段运行状态、过程实例、准入判断、事实观测结果、最终成立状态、执行轨迹和审计摘要。
3. 新增 Mock 时间轴 Schema，明确 Mock 时间轴是测试剧本 DSL，不是 Runtime 队列中的正式事件。
4. 新增倒水过程实例和三条 Mock 剧本：成功闭环、无水流失败、双通道冲突。
5. 新增 Robot Adapter 合约，明确 Adapter 只负责采样、降噪、限频、事件推送、快照返回和背压接口，不负责判断阶段跃迁。
6. 新增准入规则文档，明确第一阶段只做规则映射、模板存在性、绑定完整性、双通道验真定义和能力词汇表检查。
7. 新增 Schema 受控变更协议，将“冻结”定义为不能隐式变更，而不是绝对不能变更。
8. 扩展阶段零校验脚本，对全部 JSON 解析、倒水阶段顺序、能力词汇表、Mock 时间基准、双通道成立/冲突和失败事件进行门禁检查。

## 产出文件

1. `schemas/runtime_event.schema.json`
2. `schemas/stage_runtime_state.schema.json`
3. `schemas/process_instance.schema.json`
4. `schemas/fact_observation_result.schema.json`
5. `schemas/final_establishment_state.schema.json`
6. `schemas/admission_decision.schema.json`
7. `schemas/execution_trace.schema.json`
8. `schemas/audit_summary.schema.json`
9. `schemas/mock_timeline.schema.json`
10. `demo_runtime/rell_sample/README.md`
11. `demo_runtime/rell_sample/validate_stage_zero.py`
12. `demo_runtime/rell_sample/docs/adapter_contract.md`
13. `demo_runtime/rell_sample/docs/admission_rules.md`
14. `demo_runtime/rell_sample/docs/schema_change_protocol.md`
15. `demo_runtime/rell_sample/data/pour_water_task_intent.json`
16. `demo_runtime/rell_sample/data/pour_water_process_instance.json`
17. `demo_runtime/rell_sample/data/stage_runtime_state_initial.json`
18. `demo_runtime/rell_sample/data/admission_decision_allowed.json`
19. `demo_runtime/rell_sample/data/mock_timeline_success.json`
20. `demo_runtime/rell_sample/data/mock_timeline_no_flow.json`
21. `demo_runtime/rell_sample/data/mock_timeline_channel_conflict.json`
22. `研发记录/证据哈希/2026-07-09_RELL样品阶段零证据哈希清单.txt`

## 验证方式

1. 执行阶段零校验脚本：`python .\demo_runtime\rell_sample\validate_stage_zero.py`
2. 使用 Python 按 UTF-8 解析 `schemas/` 与 `demo_runtime/rell_sample/data/` 下所有 JSON 文件。
3. 使用 `git status --short` 检查本次改动范围，避免将 P016 全量内部材料混入公开代码边界。
4. 使用 SHA256 固定本次阶段零主要产出文件哈希。

## 验证结果

1. 阶段零校验通过，输出：`Stage zero validation passed.`
2. 校验范围包括：全部 JSON 解析、Schema 文件存在性、过程实例、准入判断、初始状态、成功/无水流/冲突三条 Mock 时间轴。
3. UTF-8 JSON 解析通过，共 44 个 JSON 文件。
4. 当前仍处于阶段零，尚未开始完整 Runtime、API 服务和前端可视化实现。
5. 已生成 `研发记录/证据哈希/2026-07-09_RELL样品阶段零证据哈希清单.txt`。

## 问题与风险

1. 当前未安装 `jsonschema`，阶段零校验脚本采用轻量自校验方式，后续可在开发依赖确定后接入标准 JSON Schema 校验。
2. Mock 时间轴仍是剧本 DSL，后续 Runtime 实现时必须由 MockRobotAdapter 展开为正式 `RuntimeEvent`。
3. 第一阶段不应扩展到多动作过程链、根因诊断、自动模板学习、真实视觉识别或真实流体仿真。
4. `技术库/` 与 `研发记录/` 属于私有证据链，不进入公开 GitHub 仓库。

## 下一步计划

1. 将 Runtime 最小实现纳入代码证据链。
2. 继续实现第一阶段最小外部 API：`/process/admit`、`/process/run`、`/audit/{task_id}`。
3. 增加纯文本或 JSON 状态流演示界面，避免第一阶段过早投入 3D 或复杂前端。
4. 后续再进入仿真平台或机器人 SDK 适配。

## 追加研发过程：Runtime 最小实现

1. 新增 `SerialEventQueue`，所有 MockAdapter 推送事件先进入队列，Runtime 单线程串行消费事件，避免异步事件并发修改状态。
2. 新增 `MockRobotAdapter`，按阶段启动时间读取 Mock 时间轴，并将剧本事件展开为 `RuntimeEvent`。
3. 新增 `P016Runtime`，实现准入判断、阶段运行状态、连续状态变量更新、阶段跃迁判断、事实观测聚合、最终成立状态计算和人工确认降级。
4. 新增 Runtime 样品运行脚本，一次运行成功、无水流失败、双通道冲突三条路径。
5. 新增 Runtime 样品校验脚本，固定三条路径的预期输出。
6. 更新代码-技术对应证据链，补入 P016 Runtime 样品对应表。

## 追加产出文件

1. `demo_runtime/rell_sample/runtime_core.py`
2. `demo_runtime/rell_sample/run_runtime_sample.py`
3. `demo_runtime/rell_sample/validate_runtime_sample.py`
4. `技术库/代码-技术对应证据链.md`
5. `研发记录/证据哈希/2026-07-09_RELL样品Runtime最小实现证据哈希清单.txt`

## 追加验证方式

1. 执行 `python .\demo_runtime\rell_sample\validate_stage_zero.py`
2. 执行 `python .\demo_runtime\rell_sample\validate_runtime_sample.py`
3. 执行 `python .\demo_runtime\rell_sample\run_runtime_sample.py`

## 追加验证结果

1. 阶段零校验通过。
2. Runtime 样品校验通过。
3. `success` 路径输出 `outcome=completed`，`runtime_state=completed`，`cup_has_water=established`。
4. `no_flow` 路径输出 `outcome=requires_human_confirmation`，`runtime_state=awaiting_human_confirmation`。
5. `channel_conflict` 路径输出 `outcome=requires_human_confirmation`，`runtime_state=awaiting_human_confirmation`，`cup_has_water=conflicted`。

## 第二次追加研发过程：最小 API 壳

1. 新增 `api_server.py`，使用标准库 HTTP 服务暴露第一阶段最小外部 API。
2. 新增 `POST /process/admit`，用于输出准入判断结果。
3. 新增 `POST /process/run`，用于运行单过程倒水实例并返回 `task_id`、状态快照和审计摘要。
4. 新增 `GET /audit/{task_id}`，用于查询本进程内最近运行产生的审计摘要。
5. 保留 `GET /process/status/{task_id}` 作为调试接口，不作为对外承诺接口。
6. 新增 `validate_api_sample.py`，对 admit、run 和 audit 查询做函数级校验。
7. 新增 `docs/api_contract.md`，固定第一阶段 API 边界。

## 第二次追加产出文件

1. `demo_runtime/rell_sample/api_server.py`
2. `demo_runtime/rell_sample/validate_api_sample.py`
3. `demo_runtime/rell_sample/docs/api_contract.md`
4. `研发记录/证据哈希/2026-07-09_RELL样品最小API证据哈希清单.txt`

## 第二次追加验证结果

1. `python .\demo_runtime\rell_sample\validate_api_sample.py` 通过。
2. 本地 HTTP 服务验证通过：`GET /health` 返回 `ok`。
3. `POST /process/admit` 返回 `allowed`。
4. `POST /process/run` 使用 `channel_conflict` 场景返回 `requires_human_confirmation`。
5. `GET /audit/task_pour_water_001` 返回同一审计结果。

## 关联 Git 提交

待随“建立RELL样品阶段零Schema与Mock门禁”提交固定。
