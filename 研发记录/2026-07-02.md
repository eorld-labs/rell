# 研发记录：2026-07-02

## 研发主题

修正 GitHub 开源仓库发布边界，移除公开仓库中的内部技术库和研发记录，仅保留第一期开源代码骨架。

## 参与人/执行主体

- 项目负责人：用户
- 工程协作：Codex

## 今日目标

1. 确认 GitHub 开源仓库不应包含内部技术库、研发记录和专利原始材料。
2. 将 `eorld-labs/rell` 修正为只包含第一期开源代码和 demo。
3. 恢复内部仓库远端关系，避免后续误推。
4. 建立 GitHub 开源发布边界与流程文档。

## 对应技术层

- [x] 空间经验世界模型层
- [x] 运行时执行治理层
- [ ] 空间治理与对象秩序层

## 对应专利/技术链特征

1. P010-P015：第一期开源只围绕六个经验闭环的 schema 和 demo。
2. P005-P007：完整空间治理能力不进入 GitHub 开源仓库。
3. 工程证据链：内部研发记录继续保留在 Gitee 内部仓库，不进入 GitHub。
4. 开源边界：GitHub 只保留 `README.md`、`schemas/`、`demo_runtime/` 和 `.gitignore`。

## 输入材料

1. 当前内部仓库 `D:\worldmodels`。
2. GitHub 远端 `git@github-eorld-labs:eorld-labs/rell.git`。
3. 第一期开源骨架：`README.md`、`schemas/`、`demo_runtime/`。
4. 用户对开源仓库边界的修正意见。

## 研发过程

1. 发现 GitHub 远端曾被推入内部完整仓库内容，包括 `技术库/` 和 `研发记录/`。
2. 判断该内容不适合作为公开开源仓库内容，应立即修正。
3. 修改根 `README.md`，移除对内部 `技术库/` 和 `研发记录/` 的引用。
4. 创建临时发布仓库，只复制 `README.md`、`.gitignore`、`schemas/` 和 `demo_runtime/`。
5. 在临时发布仓库中创建单根提交 `09adbbd 初始化真实世界经验闭环开源骨架`。
6. 强制推送覆盖 GitHub `master`，移除公开分支上的内部历史引用。
7. 将内部仓库远端恢复为 `origin -> Gitee`，新增 `github-open -> GitHub`，避免后续误推。
8. 克隆 GitHub 远端进行检查，确认公开文件列表仅包含开源白名单内容。
9. 新增 `技术库/GitHub开源仓库发布边界与流程.md`，固化后续发布规则。

## 产出文件

1. `README.md`
2. `技术库/GitHub开源仓库发布边界与流程.md`
3. `研发记录/2026-07-02.md`

## 验证方式

1. 执行临时发布仓库 `git push --force origin master`。
2. 执行 `git ls-remote github-open` 检查 GitHub 当前提交。
3. 克隆 `git@github-eorld-labs:eorld-labs/rell.git` 检查公开文件列表。
4. 执行 `git remote -v` 检查内部仓库远端关系。
5. 执行 `git status --short --branch` 检查内部仓库状态。

## 验证结果

1. GitHub `master` 已被强制覆盖为单根开源提交。
2. GitHub 当前提交为 `09adbbd 初始化真实世界经验闭环开源骨架`。
3. GitHub 公开文件仅包括 `.gitignore`、`README.md`、`demo_runtime/` 和 `schemas/`。
4. 内部仓库 `origin` 已恢复为 Gitee。
5. GitHub 开源仓库已改为 `github-open` 远端，避免误推。

## 问题与风险

1. GitHub 公开分支已被覆盖，但此前短时间公开过内部历史；若仓库为公开状态，仍需注意平台缓存和已访问风险。
2. 后续不得直接从内部仓库向 GitHub 推送完整分支。
3. GitHub 发布必须使用临时白名单发布流程。

## 下一步计划

1. 后续开源内容按白名单发布。
2. 为开源仓库补充独立 `LICENSE`、`CONTRIBUTING.md`、`docs/`、`examples/` 和 `evals/`。
3. 每次开源发布前执行文件列表检查。

## 关联 Git 提交

待本次内部仓库提交完成后补入提交号。

