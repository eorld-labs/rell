# 2026-07-10 研发记录

## 第二十次追加研发过程：P014 执行恢复记录主链补强

1. 基于“P011/P013/P014/P015 中最薄弱的是 P014 执行恢复证据链”的复核判断，将恢复逻辑从旧 `run_skill_cocreation.py` 的静态补救记录，升级为 `demo_runtime/rell_sample/` 主链内的动态恢复记录闭环。
2. 在 `demo_runtime/rell_sample/api_server.py` 中新增 `RECOVERY_LIBRARY_FILE`、`load_recovery_library()`、`save_recovery_library()`、`build_recovery_action()`、`build_recovery_outcome_type()`、`persist_recovery_record()`、`attach_recovery_record_to_context()`、`build_recovery_record_for_task()`、`get_recovery_record()` 和 `get_recovery_records_for_task()`。
3. 新增 `demo_runtime/rell_sample/data/recovery_record_library.json`，作为恢复记录的持久化载体，固定恢复记录结构和样品证据路径。
4. 在 `run_process_chain_experience()` 中补入步骤前检查阻断的恢复记录生成逻辑；当因前提事实、本体能力或空间绑定问题导致 `step_preflight_blocked` 时，不直接丢弃异常，而是沉淀为可查询恢复记录。
5. 在 `readapt_runtime_conflict()` 中补入运行时冲突再适配的恢复记录生成逻辑，使“执行冲突 -> 重新适配”之间留有单独的恢复证据。
6. 在 `dispatch_execution_loop_payload()` 中补入执行闭环中途阻断的恢复记录生成逻辑，使开放执行接口返回的阻断、失败或冲突不只体现在执行结果，还能沉淀为恢复记录。
7. 在 `run_process()` 中对 `channel_conflict`、`no_flow` 等非 `completed` 结果统一生成恢复记录，补齐过去“异常有结果、但没有恢复证据实体”的缺口。
8. 新增 `GET /recovery/library`、`GET /recovery/task/{task_id}` 和 `GET /recovery/{recovery_id}` 三个接口，使恢复记录具备按库、按任务和按单条标识查询的工程取证入口。
9. 更新 `demo_runtime/rell_sample/docs/api_contract.md`、`技术库/RELL样品开发执行计划.md` 和 `技术库/代码-技术对应证据链.md`，将本轮恢复记录主链补强同步写入接口文档、开发计划和技术证据链说明。

## 第二十次追加产出文件

1. `demo_runtime/rell_sample/api_server.py`
2. `demo_runtime/rell_sample/data/recovery_record_library.json`
3. `demo_runtime/rell_sample/docs/api_contract.md`
4. `demo_runtime/rell_sample/validate_api_sample.py`
5. `demo_runtime/rell_sample/run_all_checks.py`
6. `技术库/RELL样品开发执行计划.md`
7. `技术库/代码-技术对应证据链.md`
8. `研发记录/2026-07-10.md`

## 第二十次追加验证方式

1. 运行 `python demo_runtime\rell_sample\validate_api_sample.py`。
2. 运行 `python demo_runtime\rell_sample\run_all_checks.py`。

## 第二十次追加验证结果

1. `validate_api_sample.py` 已通过，新增覆盖恢复记录按标识查询、按任务查询、恢复记录库持久化和相关返回结构校验。
2. `run_all_checks.py` 已通过，HTTP smoke 已覆盖 `GET /recovery/library`、`GET /recovery/task/{task_id}` 和 `GET /recovery/{recovery_id}`。
3. 本轮证明 P014 在 `demo_runtime/rell_sample/` 中已经从“异常说明”升级为“恢复记录实体 + 查询接口 + 审计关联 + 再适配入口”的工程闭环。

## 第二十一次追加研发过程：P013 任务语义翻译工程证据收束

1. 基于“P013 实际能力已经进入主链，但缺少独立可举证的工程包”的判断，新增 `demo_runtime/rell_sample/validate_p013_task_semantics.py`，把 P013 从散落在综合校验脚本里的断言收束为独立证据链。
2. 固定输出目录 `demo_runtime/output/rell_sample/p013_task_semantics/`，生成 `00_summary.json`、`evidence_index.json` 和六个分段证据文件，分别对应统一语义入口、目标事实与过程链、显式路线保真、教学反馈闭环、统一入口与 LLM 边界、任务经验库沉淀。
3. 证据脚本覆盖 `build_semantic_request_frame()`、`translate_intent()`、`solve_causal_process_chain()`、`build_explicit_causal_plan()`、`teach_experience_from_dialogue()`、`start_teaching_session()`、`execute_teaching_session_step()`、`finish_teaching_session()`、`handle_agent_query()`、`resolve_concepts_for_intent()` 和 `build_llm_prompt_contract()` 等关键主链函数。
4. 新增 `技术库/P013-任务语义翻译/P013任务语义翻译工程证据说明.md`，将中文摘要、验证命令、输出目录、关键证据文件和典型审查质疑对应关系固定为可直接翻阅材料。
5. 更新 `demo_runtime/rell_sample/run_all_checks.py`，将 `validate_p013_task_semantics.py` 接入一键回归主链，使 P013 与 P014、P017 一样具备独立刷新和整体验收入口。
6. 更新 `技术库/RELL样品开发执行计划.md` 和 `技术库/代码-技术对应证据链.md`，使计划与总证据链同步纳入本轮 P013 工程收束结果。

## 第二十一次追加产出文件

1. `demo_runtime/rell_sample/validate_p013_task_semantics.py`
2. `demo_runtime/rell_sample/run_all_checks.py`
3. `demo_runtime/output/rell_sample/p013_task_semantics/`
4. `技术库/P013-任务语义翻译/P013任务语义翻译工程证据说明.md`
5. `技术库/RELL样品开发执行计划.md`
6. `技术库/代码-技术对应证据链.md`
7. `研发记录/2026-07-10.md`

## 第二十一次追加验证方式

1. 运行 `python demo_runtime\rell_sample\validate_p013_task_semantics.py`。
2. 运行 `python demo_runtime\rell_sample\run_all_checks.py`。

## 第二十一次追加验证结果

1. `validate_p013_task_semantics.py` 应生成 P013 任务语义翻译工程证据目录，并完成统一语义入口、目标事实翻译、显式路线保真、教学反馈和任务经验记录断言。
2. `run_all_checks.py` 应将 `validate_p013_task_semantics.py` 纳入全量校验主链，与现有 Stage Zero、Runtime、API、P014、P017 一起通过。

## 第十六次追加研发过程：大模型只读上下文与候选输出校验预留层

1. 在不改变“任务意图 + 空间语义 + 本体能力 + 编排层 + 任务期快照”主路线的前提下，新增未来大模型接入的薄接口层，明确模型只负责理解、澄清、解释和候选建议，不负责连续控制或事实写回。
2. 在 `api_server.py` 中新增 `build_llm_context_view()`，严格从当前任务期运行时世界状态快照生成只读语义视图；若快照已释放，则返回 `snapshot_released`，不再把该快照当作当前世界状态提供给模型。
3. 新增 `build_llm_candidate_intent()`，把 `semantic_request`、`intent_translation_preview`、可选的 `runtime_context_view` 和模型输入约束打包为未来模型调用前的标准输入。
4. 新增 `validate_llm_candidate_output()`，对模型候选输出做确定性校验，禁止绝对坐标、关节角、轨迹、运行时事实写回字段和底层控制字段，并要求候选计划必须回到编排层，不能直接执行。
5. 新增 `POST /llm/context-view`、`POST /llm/candidate-intent` 和 `POST /llm/candidate/validate` 三个样品接口，并更新函数级与 HTTP 级验证，证明这层预留已可以独立回归。

## 第十六次追加产出文件

1. `demo_runtime/rell_sample/api_server.py`
2. `demo_runtime/rell_sample/docs/api_contract.md`
3. `demo_runtime/rell_sample/validate_api_sample.py`
4. `demo_runtime/rell_sample/run_all_checks.py`
5. `研发记录/2026-07-10.md`

## 第十六次追加验证方式

1. 运行 `python demo_runtime\\rell_sample\\validate_api_sample.py`。
2. 运行 `python demo_runtime\\rell_sample\\run_all_checks.py`。

## 第十五次追加研发过程：统一自然语言主线收口到任务执行

1. 将前端 DEMO 的“运行任务”入口从 `/intent/translate + /process/admit + /process/run` 拆分调用，收口到统一的 `POST /agent/query` 自然语言入口。
2. 在 `handle_agent_query()` 中补强 `task_execution` 预览路径：当 `auto_execute=false` 时返回 `intent_translation` 和 `space_admission`；当 `auto_execute=true` 时再进入 `run_process()` 执行链路，使同一入口同时承接语义理解与执行控制。
3. 更新 `runProcess()` 前端逻辑，先调用 `/agent/query` 获取语义路由、意图翻译和准入结果，再以同一接口触发 `auto_execute=true`，使任务执行与状态提问共享同一语义主线。
4. 更新 `validate_api_sample.py` 和 `run_all_checks.py`，新增对 `/agent/query` 的 `task_execution` 预览和 `auto_execute` 执行验证，确保新主链在函数级和 HTTP 级都可重复验收。

## 第十五次追加产出文件

1. `demo_runtime/rell_sample/api_server.py`
2. `demo_runtime/rell_sample/docs/api_contract.md`
3. `demo_runtime/rell_sample/validate_api_sample.py`
4. `demo_runtime/rell_sample/run_all_checks.py`
5. `技术库/RELL样品开发执行计划.md`
6. `研发记录/2026-07-10.md`

## 第十五次追加验证方式

1. 运行 `python demo_runtime\\rell_sample\\validate_api_sample.py`。
2. 运行 `python demo_runtime\\rell_sample\\run_all_checks.py`。

## 研发主题

P017 跨空间跨本体真实经验迁移的业务逻辑剥离攻防补强。

## 参与人或执行主体

Codex。

## 今日目标

在 P017 已完成权利要求 v3、竞对规避设计攻防和无效宣告攻防补强的基础上，补齐“业务逻辑剥离”视角，防止后续说明书被读成普通案例复用、规则模板、设备能力匹配或任务状态管理。

## 对应技术层

空间经验世界模型层、运行时执行治理层。

## 对应专利/技术链特征

1. 经验不变量契约。
2. 当前空间语义数据。
3. 本体能力画像。
4. 任务期运行时世界状态快照。
5. 执行可行性结果。
6. 执行闭环事实回传和任务后释放。

## 输入材料

1. `技术库/P017-跨空间跨本体的真实经验迁移/权利要求书-v3-6段最小闭环母案版.md`
2. `技术库/P017-跨空间跨本体的真实经验迁移/说明书提纲-v1-含分案种子.md`
3. `技术库/P017-跨空间跨本体的真实经验迁移/竞对规避设计攻防-v1.md`
4. `技术库/P017-跨空间跨本体的真实经验迁移/无效宣告攻防补强-v1.md`
5. `技术库/P016-物理经验学习与泛化方法/P016-业务逻辑剥离攻防分析.md`

## 研发过程

1. 对照 P016 的业务逻辑剥离攻防结构，识别 P017 的差异化风险：P017 的风险重点不是“物理动作流程模板被说成业务流程”，而是“真实经验迁移适配被说成案例检索、规则模板、上下文状态或设备配置匹配”。
2. 固定 P017 的技术性支点：经验不变量契约、当前空间语义数据、本体能力画像、任务期运行时世界状态快照、执行可行性结果和执行闭环事实回传更新。
3. 形成七类攻击路径和对应防守口径：经验记录是历史业务案例、经验不变量契约是业务规则、空间语义是上下文参数、本体能力画像是设备配置表、任务期快照是任务状态表、执行可行性结果是业务判断、执行闭环返回是日志回写。
4. 将攻防结论落入说明书提纲，新增“业务逻辑剥离视角的提纲补强”，便于正式说明书起草时直接吸收。

## 产出文件

1. `技术库/P017-跨空间跨本体的真实经验迁移/业务逻辑剥离攻防分析-v1.md`
2. `技术库/P017-跨空间跨本体的真实经验迁移/说明书提纲-v1-含分案种子.md`
3. `研发记录/2026-07-10.md`
4. `研发记录/证据哈希/2026-07-10_P017业务逻辑剥离攻防证据哈希清单.txt`

## 验证方式

1. 使用全文检索确认新增业务逻辑剥离内容已进入 P017 目录。
2. 检查说明书提纲末尾新增章节结构是否完整。
3. 检查工作区状态，确认只新增或修改本轮相关文档。

## 验证结果

1. `rg` 检索已能在 P017 目录中定位 `业务逻辑剥离攻防分析-v1.md` 及说明书提纲第十三章。
2. `说明书提纲-v1-含分案种子.md` 末尾已新增“十三、业务逻辑剥离视角的提纲补强”，覆盖反业务流程口径、技术问题写法、技术手段写法、术语边界、实施例写法、审查答复口径和对权利要求的影响。
3. 已生成本轮三个核心产出文件的 SHA256 证据哈希清单。
4. `git status --short` 显示本轮新增文档处于未跟踪状态；P017 目录中此前生成的多份文档也仍处于未跟踪状态，未执行提交。

## 问题与风险

1. P017 的说明书正式稿仍需补充倒水端到端数据流、实施例矩阵和术语边界表。
2. 业务逻辑剥离防线不宜进入独权过多，否则会稀释当前六段最小闭环。
3. 后续正式说明书初稿成稿后，需要重新生成说明书初稿对应的证据哈希清单。

## 下一步计划

1. 将说明书提纲扩展为正式说明书初稿。
2. 补充倒水、抓取、插入、导航四类实施例的“错误存法/正确存法”表。
3. 根据正式说明书反查权利要求 v3 是否需要文字级收口。

## 关联 Git 提交

待提交。

## 第二次追加研发过程：P017 实施主体与接口拆分攻防加固

1. 对照 `技术库/P016-物理经验学习与泛化方法/P016-实施主体与接口拆分攻防分析.md`，识别 P017 的分布式实施风险。
2. 明确 P017 在商业落地中可能被拆成云端经验库、空间语义服务、本体能力服务、边缘迁移适配器、机器人端执行闭环、外部审计系统和 SDK/API。
3. 新增 `实施主体与接口拆分攻防分析-v1.md`，覆盖八类规避路径：云端经验记录、第三方空间语义、SDK 本体能力画像、边缘端任务期快照、API 过滤式可行性结果、外部执行闭环、外部审计记录以及只提供 SDK/API。
4. 将攻防结论落入说明书提纲，新增“十四、实施主体与接口拆分视角的提纲补强”，固定分布式实施总口径、迁移适配控制流程、SDK/API 产品形态、模块外置不影响闭环、接口化解释和云边端协同实施例。

## 第二次追加产出文件

1. `技术库/P017-跨空间跨本体的真实经验迁移/实施主体与接口拆分攻防分析-v1.md`
2. `技术库/P017-跨空间跨本体的真实经验迁移/说明书提纲-v1-含分案种子.md`
3. `研发记录/2026-07-10.md`
4. `研发记录/证据哈希/2026-07-10_P017实施主体与接口拆分攻防证据哈希清单.txt`

## 第二次追加验证方式

1. 使用全文检索确认新增实施主体与接口拆分内容已进入 P017 目录。
2. 检查说明书提纲末尾新增第十四章结构是否完整。
3. 生成本轮核心产出文件的 SHA256 证据哈希清单。

## 第二次追加验证结果

1. `rg` 检索已能在 P017 目录中定位 `实施主体与接口拆分攻防分析-v1.md` 及说明书提纲第十四章。
2. `说明书提纲-v1-含分案种子.md` 末尾已新增“十四、实施主体与接口拆分视角的提纲补强”，覆盖分布式实施总口径、迁移适配控制流程、SDK/API 产品形态、模块外置不影响闭环、接口化解释、云边端协同实施例和对权利要求的影响。
3. 已生成本轮三个核心产出文件的 SHA256 证据哈希清单。

## 第三次追加研发过程：剩余漏洞与权利要求类型策略复盘

1. 对业务逻辑剥离、实施主体与接口拆分、竞对规避和无效宣告四类攻防进行横向复盘。
2. 识别仍需进入说明书提纲的剩余漏洞：取证与可观测性、主控主体、真实经验边界、不保存绝对坐标/轨迹的解释边界、黑箱模型与 embedding 表示、输入源冲突和运行时能力失配、快照释放与审计保留、自家专利矩阵边界、数字执行体技术性限定、字段级接口支撑。
3. 形成当前母案权利要求类型策略：建议当前 v3 继续保持 1 条方法独权和 9 条方法从权，系统权、电子设备权、存储介质权和计算机程序产品权先放入说明书，后续由分案承接；若当前案必须加入系统权或介质权，再优先牺牲当前权利要求 9 和 10，不动权利要求 2 至 8 的核心退守层。
4. 将上述结论落入说明书提纲，新增“十五、剩余漏洞与权利要求类型策略”。

## 第三次追加产出文件

1. `技术库/P017-跨空间跨本体的真实经验迁移/说明书提纲-v1-含分案种子.md`
2. `研发记录/2026-07-10.md`
3. `研发记录/证据哈希/2026-07-10_P017剩余漏洞与权利要求类型策略证据哈希清单.txt`

## 第三次追加验证方式

1. 使用全文检索确认说明书提纲第十五章已写入。
2. 检查第十五章是否包含当前母案权利要求类型策略和必须加入当前案的备选方案。
3. 刷新相关证据哈希清单。

## 第三次追加验证结果

1. `rg` 检索已能在说明书提纲中定位“十五、剩余漏洞与权利要求类型策略”。
2. 第十五章已包含取证与可观测性、主控主体、真实经验边界、不保存绝对坐标/轨迹解释边界、黑箱模型与 embedding 表示、输入源冲突和运行时能力失配、快照释放与审计保留、自家专利矩阵边界、数字执行体技术性限定、字段级接口支撑。
3. 第十五章已写入当前母案权利要求类型策略：建议保持当前 10 条方法权结构；若当前案必须加入系统权或介质权，优先移出权利要求 9 和 10，不动权利要求 2 至 8 的核心退守层。
4. 已生成本轮说明书提纲和研发记录的 SHA256 证据哈希清单，并同步刷新本日相关哈希清单中的说明书提纲和研发记录哈希。

## 第四次追加研发过程：v4 母案权要与分案种子独权级支撑

1. 根据顾问意见和内部复盘，将母案策略从“纯方法 10 条”调整为“方法主链 + 不可执行处理 + 系统权兜底”。
2. 新增 `权利要求书-v4-10条母案系统权兜底版.md`，保留权利要求 1 至 8 的核心退守层，将 v3 权利要求 9 和 10 移入说明书强分案种子，新增不可执行/部分不可执行处理从权和迁移适配控制器系统权。
3. 在说明书提纲第九章和第十五章中同步更新当前母案布局建议。
4. 新增说明书提纲第十六章“分案种子独权级支撑清单”，把刷下来的目标因果事实反推前提链、显式空间路线保真与因果校验，以及昨天形成的 SDK/API、运行时冲突、快照释放、执行闭环开放接口、经验不变量契约生成、数字执行体迁移、不可执行到教学入口等方向，写成可直接拉独权的技术问题、主流程、数据结构、实施例和区别口径。

## 第四次追加产出文件

1. `技术库/P017-跨空间跨本体的真实经验迁移/权利要求书-v4-10条母案系统权兜底版.md`
2. `技术库/P017-跨空间跨本体的真实经验迁移/说明书提纲-v1-含分案种子.md`
3. `研发记录/2026-07-10.md`
4. `研发记录/证据哈希/2026-07-10_P017母案v4与分案独权级支撑证据哈希清单.txt`

## 第四次追加验证方式

1. 使用全文检索确认 v4 权利要求书、说明书第十六章和关键分案标题已写入。
2. 检查 v4 权要是否包含不可执行/部分不可执行处理和迁移适配控制器系统权。
3. 生成本轮核心产出文件的 SHA256 证据哈希清单。

## 第四次追加验证结果

1. `rg` 检索已能定位 v4 权利要求书、说明书提纲第十六章和第四次追加研发记录。
2. v4 权利要求书已包含不可执行/部分不可执行处理从权和迁移适配控制器系统权。
3. 说明书提纲第十六章已将九个分案方向写成技术问题、可拉独权主流程、必要数据结构、实施例和区别口径。
4. 已生成本轮三个核心产出文件的 SHA256 证据哈希清单，并同步刷新本日相关哈希清单中的说明书提纲和研发记录哈希。

## 第五次追加研发过程：P017 说明书 v1 正式初稿与顾问意见加固

1. 基于 `权利要求书-v4-10条母案系统权兜底版.md`、`说明书提纲-v1-含分案种子.md`、业务逻辑剥离攻防、实施主体与接口拆分攻防以及顾问意见，起草并加固 `说明书-v1-正式初稿.md`。
2. 针对系统权单一性和“装置化改写”风险，在说明书中补强迁移适配控制器系统实施例，突出接口单元、契约解析单元、空间绑定单元、能力匹配单元、状态管理单元、适配决策单元、执行调度单元、事实回传单元和审计释放单元之间的数据流、事件流、分布式接口协作和 SDK/API 主控主体。
3. 针对权利要求 4 的负面限定风险，新增不可迁移参数剥离实施例，将“不保存绝对坐标、关节角、固定时长、单一本体轨迹”等表述解释为积极的数据剥离处理，并明确其降低数据负担、提升跨空间跨本体泛化能力、避免环境变化或机械磨损导致直接复用失败的技术效果。
4. 针对权利要求 7 的快照释放风险，新增任务期快照释放与状态隔离实施例，明确释放不是普通计算机内存释放，而是通过快照标识、任务范围、释放状态、释放令牌和后续任务引用拦截实现防状态污染的安全控制机制。
5. 将刷下来的分案种子写入正式说明书的“分案独权级支撑实施例”，覆盖迁移适配控制器、云端服务器、SDK/API 产品、目标因果事实反推前提链、显式空间路线保真与因果校验、运行时冲突处理与重新适配、执行闭环开放接口、经验不变量契约生成、数字执行体迁移、不可执行结果到教学入口、电子设备、存储介质和程序产品。
6. 新增自家技术矩阵边界实施例，说明 P017 与 P010 空间语义/世界表示、P013 长期世界模型、P014 教学/路线示教、P016 过程模板/执行闭环之间的关系，避免后续审查和分案中自家技术方向互相挤压。

## 第五次追加产出文件

1. `技术库/P017-跨空间跨本体的真实经验迁移/说明书-v1-正式初稿.md`
2. `技术库/P017-跨空间跨本体的真实经验迁移/权利要求书-v4-10条母案系统权兜底版.md`
3. `研发记录/2026-07-10.md`
4. `研发记录/证据哈希/2026-07-10_P017说明书v1正式初稿证据哈希清单.txt`

## 第五次追加验证方式

1. 使用全文检索确认说明书中已包含系统权反装置化、积极数据剥离、快照释放非普通内存管理、不可执行/部分不可执行处理、分案独权级支撑和自家技术矩阵边界。
2. 检查说明书是否保留 v4 权利要求 1 至 10 的直接支撑，并对移入说明书的分案方向提供可直接拉独权的实施基础。
3. 生成说明书正式初稿、v4 权利要求书和研发记录的 SHA256 证据哈希清单。

## 第五次追加验证结果

1. `rg` 检索已能在 `说明书-v1-正式初稿.md` 中定位“装置化改写”“事件总线”“SDK/API”“不可迁移参数剥离”“积极的数据剥离处理”“不是普通计算机内存释放”“release_token”“不可执行或部分不可执行”“分案独权级支撑”“自家技术矩阵边界”等关键口径。
2. 正式说明书已补强系统权独立装置构思、权利要求 4 的负面限定技术效果、权利要求 7 的快照释放安全控制机制，以及权利要求 9 的不可执行/部分不可执行处理机制。
3. 正式说明书已将目标因果事实反推前提链、显式空间路线保真与因果校验、运行时冲突处理、开放执行闭环、经验不变量契约生成、数字执行体迁移、不可执行教学入口、云端服务器、SDK/API、电子设备、存储介质和程序产品写入可分案实施基础。
4. 已计算说明书正式初稿、v4 权利要求书和研发记录的 SHA256 哈希，并生成本轮证据哈希清单。

## 第六次追加研发过程：P017 说明书 v2 专利法律语言版

1. 基于 `说明书-v1-正式初稿.md` 生成 `说明书-v2-专利法律语言版.md`，保留 v1 作为技术底稿，不覆盖原文件。
2. 将内部策略性表达转换为专利说明书法律体系语言：将“分案独权级支撑实施例”调整为“其他可选实施方式”，将“自家技术矩阵边界实施例”调整为“与相关技术模块的组合实施例”，将“与其他技术方向的边界”调整为“区别说明”。
3. 清理不宜进入提交稿的内部编号和策略词，包括 P017、P016、P010、P013、P014、分案、独权、系统权、装置化、顾问等。
4. 将“不是普通”“关键在于”“不在于”等内部攻防口吻转换为“并非仅为”“区别在于”“在一些实施例中”等专利说明书常用表达。
5. 保留系统实施例、不可迁移参数剥离实施例、任务期快照释放与状态隔离实施例、其他可选实施方式以及电子设备、存储介质、程序产品实施例的实质支撑。

## 第六次追加产出文件

1. `技术库/P017-跨空间跨本体的真实经验迁移/说明书-v2-专利法律语言版.md`
2. `研发记录/2026-07-10.md`
3. `研发记录/证据哈希/2026-07-10_P017说明书v2专利法律语言版证据哈希清单.txt`

## 第六次追加验证方式

1. 使用全文检索确认 v2 说明书不含 P017、P016、P010、P013、P014、分案、独权、系统权、装置化、顾问等内部表达。
2. 使用全文检索确认 v2 说明书保留“所述经验记录”“所述当前迁移上下文”“所述任务期运行时世界状态快照”“所述执行可行性结果”“不可迁移参数剥离”“迁移适配控制器”“释放令牌”“其他可选实施方式”“区别说明”等专利支撑表达。
3. 生成 v2 说明书和研发记录的 SHA256 证据哈希清单。

## 第六次追加验证结果

1. `rg` 检索 `说明书-v2-专利法律语言版.md`，未发现 P017、P016、P010、P013、P014、分案、独权、系统权、装置化、顾问等内部表达。
2. `rg` 检索已确认 v2 说明书保留“所述经验记录”“所述当前迁移上下文”“所述任务期运行时世界状态快照”“所述执行可行性结果”“不可迁移参数剥离”“迁移适配控制器”“释放令牌”“其他可选实施方式”“区别说明”等专利支撑表达。
3. 已计算 v2 说明书和研发记录的 SHA256 哈希，并生成本轮证据哈希清单。

## 第七次追加研发过程：背景技术示教/轨迹/策略迁移段落严谨性修订

1. 对 `说明书-v2-专利法律语言版.md` 背景技术中“示教学习、模仿学习、视觉语言动作模型、轨迹模型或策略迁移”段落进行出处复核。
2. 复核 `近似检索判断-v1.md`、`中文近似摘要筛选-v1.md` 和 `中文危险全文复核-v1.md`，确认昨天检索中确有示范学习、轨迹模型、技能模型训练、跨本体策略迁移、潜在空间对齐、轨迹提示、动作令牌、目标位姿、轨迹生成等近邻。
3. 将原句中“容易把历史经验中的绝对坐标、机器人专用关节角、固定执行时长、单一本体轨迹或固定动作分布作为复用对象”的概括式表述，改为条件式表述：“若直接将历史示教或历史执行中的位姿、轨迹、关节配置、动作分布或固定执行参数作为复用依据，则可能因空间布局、执行体结构、传感器配置或对象状态变化而导致复用失败”。
4. 该修订用于区分“检索材料直接支撑的现有技术类别”和“基于该类别的技术问题推导”，避免把固定时长、绝对坐标等作为所有现有技术均明示采用的事实来表述。

## 第七次追加产出文件

1. `技术库/P017-跨空间跨本体的真实经验迁移/说明书-v2-专利法律语言版.md`
2. `研发记录/2026-07-10.md`
3. `研发记录/证据哈希/2026-07-10_P017说明书v2专利法律语言版证据哈希清单.txt`

## 第七次追加验证方式

1. 使用全文检索确认 v2 说明书中已不含“容易把”“固定动作分布作为复用对象”等过度概括表达。
2. 使用全文检索确认 v2 说明书中已替换为“若直接将历史示教或历史执行中的位姿、轨迹、关节配置、动作分布或固定执行参数作为复用依据”的条件式表达。
3. 刷新 v2 说明书和研发记录的 SHA256 证据哈希。

## 第七次追加验证结果

1. `rg` 检索确认 v2 说明书已不含“容易把”“固定动作分布作为复用对象”等过度概括表达。
2. `rg` 检索确认 v2 说明书已在背景技术中采用“若直接将历史示教或历史执行中的位姿、轨迹、关节配置、动作分布或固定执行参数作为复用依据”的条件式表达。
3. 已重新计算 v2 说明书和研发记录的 SHA256 哈希，并刷新 v2 证据哈希清单。

## 第八次追加研发过程：P017 提交后工程补强

1. 在 P017 提交后，将提交稿中最终确定的工程主链回灌到 `技术库/RELL样品开发执行计划.md`，新增“P017 提交后工程修订”章节。
2. 将后续工程优先级调整为：迁移适配控制器 / SDK / API 接口闭环、任务期快照可观测与释放证明、不可执行结果到教学入口、运行时冲突处理与重新适配、执行闭环开放接口与数字执行体迁移。
3. 在 `demo_runtime/rell_sample/api_server.py` 中新增迁移适配控制器主链函数：生成迁移任务标识、任务期运行时世界状态快照、绑定候选、执行可行性结果、开放执行闭环调用载荷和释放令牌。
4. 新增 `POST /experience/migrate`、`GET /runtime_world_state/{task_id}`、`POST /runtime_world_state/release` 三个样品 API，用于证明 P017 的迁移适配、快照查询和快照释放可观测链。
5. 在 `validate_api_sample.py` 和 `run_all_checks.py` 中加入 P017 迁移适配、不可执行/部分不可执行判断、快照释放令牌和 HTTP API 烟测。
6. 更新 `demo_runtime/rell_sample/docs/api_contract.md` 和 `技术库/P017-跨空间跨本体的真实经验迁移/代码证据映射.md`，使工程文档与提交后的 P017 判断一致。

## 第八次追加产出文件

1. `技术库/RELL样品开发执行计划.md`
2. `技术库/P017-跨空间跨本体的真实经验迁移/代码证据映射.md`
3. `demo_runtime/rell_sample/api_server.py`
4. `demo_runtime/rell_sample/docs/api_contract.md`
5. `demo_runtime/rell_sample/validate_api_sample.py`
6. `demo_runtime/rell_sample/run_all_checks.py`

## 第八次追加验证方式

1. 运行 `python demo_runtime\rell_sample\validate_api_sample.py`。
2. 运行 `python demo_runtime\rell_sample\run_all_checks.py`。

## 第八次追加验证结果

1. `validate_api_sample.py` 已通过，覆盖准入、运行、经验教学、对话教学、自然语言因果教学、显式路线教学、因果链求解、短目标求解、P017 迁移适配、任务期快照释放、冲突场景、仿真场景、审计和空间查询。
2. `run_all_checks.py` 已通过，覆盖阶段零、数字空间、本体 Adapter 契约、Runtime 样品、仿真机器人样品、API 样品和 HTTP 烟测。
3. 本轮为提交后的工程迭代记录，暂未刷新专利提交稿相关哈希清单。

## 第九次追加研发过程：P017 提交后不可执行与再适配工程闭环

1. 基于提交后的工程判断，继续补强迁移适配控制器的产品化证据，不再改动已提交的说明书和权利要求文本。
2. 在 `demo_runtime/rell_sample/api_server.py` 中新增 `EXPERIENCE_GAP_STORE` 与 `READAPTATION_STORE`，使不可执行结果和运行时再适配结果具有可查询的结构化记录。
3. 新增 `build_experience_gap_record()`，当迁移适配结果为不可执行、部分不可执行、需人工确认或需补充教学时，生成经验缺口记录，包含阻断步骤、不可执行原因、建议动作、补充教学入口、替代经验检索条件和降级执行计划。
4. 新增 `build_runtime_conflict_items()` 与 `readapt_runtime_conflict()`，从执行审计或 execution trace 中识别多通道事实冲突，将冲突写入新的任务期运行时世界状态快照，并重新生成绑定候选和执行可行性结果。
5. 新增 `GET /experience/gap/{gap_record_id}`、`POST /runtime_world_state/readapt` 和 `GET /runtime_readaptation/{readaptation_id}` 三个样品 API，用于证明不可执行教学入口和运行时冲突再适配链路。
6. 更新 `validate_api_sample.py` 和 `run_all_checks.py`，把经验缺口记录查询、运行时冲突再适配和 HTTP API 烟测纳入验收。
7. 更新 `demo_runtime/rell_sample/docs/api_contract.md`、`技术库/RELL样品开发执行计划.md` 和 `技术库/P017-跨空间跨本体的真实经验迁移/代码证据映射.md`，使计划、接口文档和代码证据与今天的工程实现一致。

## 第九次追加产出文件

1. `demo_runtime/rell_sample/api_server.py`
2. `demo_runtime/rell_sample/docs/api_contract.md`
3. `demo_runtime/rell_sample/validate_api_sample.py`
4. `demo_runtime/rell_sample/run_all_checks.py`
5. `技术库/RELL样品开发执行计划.md`
6. `技术库/P017-跨空间跨本体的真实经验迁移/代码证据映射.md`
7. `研发记录/2026-07-10.md`

## 第九次追加验证方式

1. 运行 `python demo_runtime\rell_sample\validate_api_sample.py`。
2. 运行 `python demo_runtime\rell_sample\run_all_checks.py`。

## 第九次追加验证结果

1. `validate_api_sample.py` 已通过，覆盖经验缺口记录生成与查询、运行时冲突再适配、任务期快照释放和原有 API 主链。
2. `run_all_checks.py` 已通过，覆盖阶段零、数字空间、本体 Adapter、Runtime 样品、仿真机器人样品、API 样品和 HTTP 烟测。
3. 本轮为提交后的工程迭代记录，暂未刷新专利提交稿相关哈希清单。

## 第十次追加研发过程：开放执行闭环与多类型执行器接口

1. 基于提交后工程计划的第五优先级，补强开放执行闭环和多类型执行器兼容证据。
2. 在 `demo_runtime/rell_sample/api_server.py` 中新增 `EXECUTION_DISPATCH_STORE`，用于记录执行闭环分发和事实回传结果。
3. 新增 `dispatch_execution_loop_payload()`，接收 `migrate_experience()` 返回的 `execution_loop_payload`，支持 `process_template_executor`、`digital_executor`、`simulated_robot`、`ros_controller`、`robot_sdk` 和 `vla_policy` 等执行器类型。
4. 分发执行时复用 `apply_step_to_runtime_world_state()`，依据过程步骤的 `produces_fact` 和 `destroys_facts` 更新任务期运行时世界状态快照，并返回 `fact_established`、`fact_not_established` 或 `failure` 等结构化事实状态。
5. 新增 `POST /execution/dispatch` 和 `GET /execution/dispatch/{dispatch_id}` 样品 API，用于证明迁移适配控制器能够通过开放接口调用不同底层执行模块，并接收结构化事实回传。
6. 更新 `validate_api_sample.py` 和 `run_all_checks.py`，将开放执行闭环 dispatch、事实回传、快照回写和 HTTP 查询纳入验收。
7. 更新 `demo_runtime/rell_sample/docs/api_contract.md`、`技术库/RELL样品开发执行计划.md` 和 `技术库/P017-跨空间跨本体的真实经验迁移/代码证据映射.md`，使工程计划、接口文档和代码证据与本轮实现一致。

## 第十次追加产出文件

1. `demo_runtime/rell_sample/api_server.py`
2. `demo_runtime/rell_sample/docs/api_contract.md`
3. `demo_runtime/rell_sample/validate_api_sample.py`
4. `demo_runtime/rell_sample/run_all_checks.py`
5. `技术库/RELL样品开发执行计划.md`
6. `技术库/P017-跨空间跨本体的真实经验迁移/代码证据映射.md`
7. `研发记录/2026-07-10.md`

## 第十次追加验证方式

1. 运行 `python demo_runtime\rell_sample\validate_api_sample.py`。
2. 运行 `python demo_runtime\rell_sample\run_all_checks.py`。

## 第十次追加验证结果

1. `validate_api_sample.py` 已通过，覆盖开放执行闭环 dispatch、事实回传、任务期快照回写、经验缺口、运行时再适配和原有 API 主链。
2. `run_all_checks.py` 已通过，覆盖阶段零、数字空间、本体 Adapter、Runtime 样品、仿真机器人样品、API 样品和 HTTP 烟测。
3. 本轮为提交后的工程迭代记录，暂未刷新专利提交稿相关哈希清单。

## 第十一次追加研发过程：P017 最小特征闭环证据固化

1. 基于后续审查应对需要，将 P017 的最小特征闭环从分散 API 和函数实现收束为一条可重复运行、可落盘、可复核的工程证据链。
2. 新增 `demo_runtime/rell_sample/validate_p017_minimal_loop.py`，直接调用现有迁移适配、开放执行闭环 dispatch、任务期快照查询和快照释放函数，串联形成六段闭环。
3. 固定输出目录 `demo_runtime/output/rell_sample/p017_minimal_loop/`，生成 `01_experience_record.json` 至 `06_release_and_audit.json` 六个证据文件，以及 `00_summary.json` 汇总文件。
4. 每个证据 JSON 均写入 `technical_feature` 和 `claim_1_step` 字段，用于将工程输出直接对应到 P017 方法主链中的经验记录、迁移上下文、任务期快照、跨空间跨本体适配、执行闭环事实回传、快照释放与审计。
5. 新增 `evidence_index.json`，将每个证据文件对应的技术特征、权利要求步骤、关键字段、代码来源和审查答复用途固化为机器可读索引。
6. 新增 `技术库/P017-跨空间跨本体的真实经验迁移/P017最小闭环工程证据说明.md`，以中文摘要、证据文件说明和典型审查质疑对应关系的形式，形成代理人和审查答复可直接使用的说明材料。
7. 将 `validate_p017_minimal_loop.py` 接入 `run_all_checks.py`，使后续全量样品检查自动刷新最小闭环证据。
8. 更新 `demo_runtime/rell_sample/docs/api_contract.md`、`技术库/RELL样品开发执行计划.md` 和 `技术库/P017-跨空间跨本体的真实经验迁移/代码证据映射.md`，补齐最小闭环验证命令、输出目录和证据说明。

## 第十一次追加产出文件

1. `demo_runtime/rell_sample/validate_p017_minimal_loop.py`
2. `demo_runtime/output/rell_sample/p017_minimal_loop/evidence_index.json`
3. `demo_runtime/output/rell_sample/p017_minimal_loop/00_summary.json`
4. `demo_runtime/output/rell_sample/p017_minimal_loop/01_experience_record.json`
5. `demo_runtime/output/rell_sample/p017_minimal_loop/02_migration_context.json`
6. `demo_runtime/output/rell_sample/p017_minimal_loop/03_runtime_world_state_snapshot.json`
7. `demo_runtime/output/rell_sample/p017_minimal_loop/04_binding_and_feasibility.json`
8. `demo_runtime/output/rell_sample/p017_minimal_loop/05_execution_fact_feedback.json`
9. `demo_runtime/output/rell_sample/p017_minimal_loop/06_release_and_audit.json`
10. `demo_runtime/rell_sample/run_all_checks.py`
11. `demo_runtime/rell_sample/docs/api_contract.md`
12. `技术库/RELL样品开发执行计划.md`
13. `技术库/P017-跨空间跨本体的真实经验迁移/代码证据映射.md`
14. `技术库/P017-跨空间跨本体的真实经验迁移/P017最小闭环工程证据说明.md`
15. `研发记录/2026-07-10.md`

## 第十一次追加验证方式

1. 运行 `python demo_runtime\rell_sample\validate_p017_minimal_loop.py`。
2. 运行 `python demo_runtime\rell_sample\run_all_checks.py`。

## 第十一次追加验证结果

1. `validate_p017_minimal_loop.py` 已通过，生成 `demo_runtime/output/rell_sample/p017_minimal_loop/` 下的 `evidence_index.json`、`00_summary.json` 和六个分段证据 JSON。
2. `run_all_checks.py` 已通过，且已自动执行 `validate_p017_minimal_loop.py`，覆盖 P017 最小特征闭环证据生成、阶段零、数字空间、本体 Adapter、Runtime 样品、仿真机器人样品、API 样品和 HTTP 烟测。
3. 本轮为提交后的工程证据固化，暂未刷新专利提交稿相关哈希清单。

## 第十二次追加研发过程：P017 最小闭环 DEMO 展示模式

1. 基于已固化的 P017 最小特征闭环证据包，继续打磨 DEMO 样品展示层，但展示内容限定为工程证据链，不扩散为泛泛 UI 美化。
2. 在 `demo_runtime/rell_sample/api_server.py` 中新增 `P017_MINIMAL_LOOP_OUTPUT` 和 `get_p017_minimal_loop_evidence()`，用于读取 `demo_runtime/output/rell_sample/p017_minimal_loop/` 下的 `evidence_index.json`、`00_summary.json` 和六个分段证据 JSON。
3. 新增 `GET /p017/minimal-loop` 接口，返回最小闭环证据索引、汇总和六段原始证据 JSON。
4. 在 DEMO 首页新增“P017 最小闭环”按钮和展示函数，按六段卡片展示技术特征、权利要求步骤、代码来源、审查用途、关键字段摘要和原始 JSON。
5. 更新 `run_all_checks.py` 的 HTTP 烟测，校验首页包含 `p017Button`，并校验 `/p017/minimal-loop` 能返回索引和六个证据文件。
6. 更新 `demo_runtime/rell_sample/docs/api_contract.md`、`技术库/RELL样品开发执行计划.md` 和 `技术库/P017-跨空间跨本体的真实经验迁移/代码证据映射.md`，记录展示接口和展示模式。

## 第十二次追加产出文件

1. `demo_runtime/rell_sample/api_server.py`
2. `demo_runtime/rell_sample/run_all_checks.py`
3. `demo_runtime/rell_sample/docs/api_contract.md`
4. `技术库/RELL样品开发执行计划.md`
5. `技术库/P017-跨空间跨本体的真实经验迁移/代码证据映射.md`
6. `研发记录/2026-07-10.md`

## 第十二次追加验证方式

1. 运行 `python demo_runtime\rell_sample\validate_p017_minimal_loop.py`。
2. 运行 `python demo_runtime\rell_sample\run_all_checks.py`。

## 第十二次追加验证结果

1. `validate_p017_minimal_loop.py` 已通过，最小闭环证据包可正常生成。
2. `run_all_checks.py` 已通过，HTTP 烟测已覆盖首页 `p017Button` 和 `GET /p017/minimal-loop` 接口。
3. 浏览器交互检查已通过：打开本地 DEMO 页面后点击“P017 最小闭环”，页面可渲染 6 张证据卡片，并显示技术特征、权利要求步骤、代码来源、审查用途、关键字段和原始 JSON。
4. 本轮为提交后的 DEMO 展示层工程迭代，暂未刷新专利提交稿相关哈希清单。

## 第十三次追加研发过程：数字执行主体优先与边教边动闭环

1. 基于提交后工程判断，将后续样品主线明确调整为“网页数字执行主体优先”，先跑通数字执行主体自主判断、执行、事实反馈、教学固化和快照释放，再考虑技能共创平台化、场景切换和物理引擎。
2. 在 `技术库/RELL样品开发执行计划.md` 新增“数字执行主体优先的主线备忘”，防止后续开发在技能共创台、视觉展示或物理引擎之间来回切换而忘记主线任务。
3. 在 `demo_runtime/rell_sample/api_server.py` 中新增 `TEACHING_SESSION_STORE`，用于记录边教边动会话、目标事实、已执行过程链、每一步事实反馈和任务期运行时世界状态快照。
4. 新增 `POST /teaching/session/start`、`POST /teaching/session/step`、`POST /teaching/session/finish` 和 `GET /teaching/session/{session_id}`，形成“教一步、数字执行主体动一步、立即返回因果事实反馈、成功后固化经验”的最小闭环。
5. 单步教学复用 `apply_step_to_runtime_world_state()`，并在前提事实缺失时返回 `needs_more_teaching`、缺失事实和建议先教学步骤，不强行写入产出事实。
6. 教学结束时复用 `teach_experience()` 固化经验记录，生成 `causal_signature` 和 `invariant_contract`，并将来源标记为 `stepwise_teaching_session`；随后释放任务期快照并返回 `release_token`。
7. DEMO 首页新增“边教边动”控制区，可启动会话、执行本次教学步骤、确认成功并入库，右侧继续复用数字空间和事实面板展示即时反馈。

## 第十三次追加产出文件

1. `demo_runtime/rell_sample/api_server.py`
2. `demo_runtime/rell_sample/docs/api_contract.md`
3. `demo_runtime/rell_sample/validate_api_sample.py`
4. `demo_runtime/rell_sample/run_all_checks.py`
5. `技术库/RELL样品开发执行计划.md`
6. `研发记录/2026-07-10.md`

## 第十三次追加验证方式

1. 运行 `python demo_runtime\rell_sample\validate_api_sample.py`。
2. 运行 `python demo_runtime\rell_sample\run_all_checks.py`。

## 第十三次追加验证结果

1. `validate_api_sample.py` 已通过，覆盖边教边动会话启动、前提缺失阻断、逐步执行、目标事实达成、经验固化和任务期快照释放。
2. `run_all_checks.py` 已通过，覆盖阶段零、数字空间、本体 Adapter、Runtime 样品、仿真机器人样品、API 样品、P017 最小特征闭环证据生成和 HTTP 烟测。
3. HTTP 烟测已覆盖 DEMO 首页边教边动按钮、`POST /teaching/session/start`、`POST /teaching/session/step`、`GET /teaching/session/{session_id}` 和 `POST /teaching/session/finish`。

## 第十四次追加研发过程：统一语义路由器骨架

1. 基于“家务机器人后续不能靠不断加 if-else 扩语义”的判断，在样品中新增统一语义路由器骨架，用于把自然语言先归入固定请求类型，再进入既有执行主链。
2. 在 `demo_runtime/rell_sample/api_server.py` 中新增 `build_semantic_request_frame()`，将输入统一归入 `state_query`、`task_execution`、`teaching`、`clarification` 或 `unknown`，并输出 `route_reason`、`preferred_model_tier` 和统一 `intent_frame`。
3. 新增 `POST /semantic/route`，只做语义分流，不执行任务；新增 `handle_agent_query()` 与 `POST /agent/query`，在分流后复用当前状态查询、任务翻译/执行、教学解析和原因说明能力。
4. 当前状态问答继续坚持“只读任务期运行时世界状态快照”的原则；复杂语义暂不直接接入 GPU 或大模型，但在 `preferred_model_tier` 中明确标出 `llm_escalatable` 升级位。
5. DEMO 首页“状态提问”入口改走 `/agent/query`，使前台交互和后端语义路由使用同一条自然语言主线。
6. 更新 `validate_api_sample.py` 和 `run_all_checks.py`，新增统一语义路由、状态问答、教学分流和原因说明的函数级与 HTTP 校验。

## 第十四次追加产出文件

1. `demo_runtime/rell_sample/api_server.py`
2. `demo_runtime/rell_sample/docs/api_contract.md`
3. `demo_runtime/rell_sample/validate_api_sample.py`
4. `demo_runtime/rell_sample/run_all_checks.py`
5. `技术库/RELL样品开发执行计划.md`
6. `研发记录/2026-07-10.md`

## 第十四次追加验证方式

1. 运行 `python demo_runtime\rell_sample\validate_api_sample.py`。
2. 运行 `python demo_runtime\rell_sample\run_all_checks.py`。

## 第十四次追加验证结果

1. `validate_api_sample.py` 已通过，覆盖统一语义路由对 `state_query`、`teaching`、`clarification` 的分类，以及 `POST /agent/query` 对状态问答、教学分流和原因说明的处理。
2. `run_all_checks.py` 已通过，HTTP 烟测已覆盖 `POST /semantic/route`、`POST /agent/query` 和原有样品主链。

## 第十五次追加研发过程：LLM 稳定提示词契约与概念层骨架

1. 基于“LLM 只负责长程或陌生任务语义理解，不进入连续控制与最终执行判断”的架构判断，在 `demo_runtime/rell_sample/api_server.py` 中新增 `build_llm_prompt_contract()`，把语义请求、候选意图预览、概念层解析摘要、可选的当前任务期快照视图、允许输出字段和回编排层要求打包为统一契约。
2. 新增 `POST /llm/prompt-contract`，作为后续模型调用层的稳定输入边界；该接口不调用模型，只输出契约，固定要求候选结果进入 `POST /llm/candidate/validate` 后再回到编排层。
3. 新增 `demo_runtime/rell_sample/data/concept_library.json` 和 `schemas/concept_unit.schema.json`，把概念层明确建模为可审阅的公共语义单元，而不再只是代码内嵌标签。
4. 概念单元当前包含“空间目标导航”“可交互对象获取”“可盛装容器”“水源资源区”“液体转移任务”等五类，均明确 `effect_contract`、`applicability_constraints`、`runtime_contingency_hints` 和 `experience_link_policy`，并统一声明 `direct_execution_allowed=false`。
5. 新增 `GET /concept/library` 与 `POST /concept/resolve`，用于查询概念库和把自然语言请求解析为概念层候选；解析结果带有 `activation_reason`、`runtime_binding_status` 和“必须重新进入编排层”的边界说明。
6. `build_llm_candidate_intent()` 现已携带 `llm_prompt_contract` 与 `concept_resolution`，确保未来即使接入更大模型，也只能在统一语义主链上产出候选，而不能直接下发动作或写回任务期运行时世界状态。
7. 更新 `demo_runtime/rell_sample/docs/api_contract.md`、`技术库/RELL样品开发执行计划.md` 和校验脚本，使本轮“LLM 只做语义理解 + 概念层作为公共复用中间层”的判断落到接口、文档和回归检查中。

## 第十五次追加产出文件

1. `demo_runtime/rell_sample/api_server.py`
2. `demo_runtime/rell_sample/data/concept_library.json`
3. `demo_runtime/rell_sample/docs/api_contract.md`
4. `demo_runtime/rell_sample/validate_api_sample.py`
5. `demo_runtime/rell_sample/run_all_checks.py`
6. `schemas/concept_unit.schema.json`
7. `技术库/RELL样品开发执行计划.md`
8. `研发记录/2026-07-10.md`

## 第十五次追加验证方式

1. 运行 `python demo_runtime\rell_sample\validate_api_sample.py`。
2. 运行 `python demo_runtime\rell_sample\run_all_checks.py`。

## 第十五次追加验证结果

1. `validate_api_sample.py` 已通过，新增覆盖 `build_llm_prompt_contract()`、`load_concept_library()`、`resolve_concepts_for_intent()` 及概念层边界检查。
2. `run_all_checks.py` 已通过，HTTP 烟测新增覆盖 `GET /concept/library`、`POST /concept/resolve` 和 `POST /llm/prompt-contract`。
3. 当前样品已具备“统一语义入口 -> 当前任务期快照只读视图 -> 概念层候选 -> LLM 候选契约 -> 校验器 -> 编排层”的最小主链，为后续继续加厚数字执行主体和边教边动闭环提供稳定边界。

## 第十六次追加研发过程：经验到概念候选再到人工确认晋升

1. 基于“概念层不能只是一份静态词典，必须能从成功经验中生长出来”的判断，在 `demo_runtime/rell_sample/api_server.py` 中新增概念晋升候选闭环。
2. 经验成功形成后，系统会依据经验记录、因果签名和经验不变量契约自动生成 `concept_promotion_candidates`，其中至少包括一个“新建晋升任务概念”候选，以及若干“补强现有公共概念”候选。
3. 新增 `demo_runtime/rell_sample/data/concept_candidate_library.json` 和 `schemas/concept_candidate.schema.json`，把概念晋升候选单独持久化，而不是直接写入概念库。
4. 新增 `GET /concept/candidates` 与 `POST /concept/candidates/confirm`。前者用于查询待人工确认的概念候选；后者用于把概念候选正式写入概念库，或把真实经验证据补到既有概念上。
5. 已确认晋升的概念会带上 `promotion_tags`、`derived_from_experiences` 和 `promotion_history` 等信息，并可在后续 `POST /concept/resolve` 中再次被匹配，证明“经验 -> 概念候选 -> 人确认晋升 -> 后续复用”的主链已经闭合。
6. DEMO 教学结果现会同步展示概念候选标识和确认方式，作为后续把“边教边动”延伸到“边教边沉淀概念”的第一版工程落点。

## 第十六次追加产出文件

1. `demo_runtime/rell_sample/api_server.py`
2. `demo_runtime/rell_sample/data/concept_candidate_library.json`
3. `demo_runtime/rell_sample/docs/api_contract.md`
4. `demo_runtime/rell_sample/run_all_checks.py`
5. `demo_runtime/rell_sample/validate_api_sample.py`
6. `schemas/concept_candidate.schema.json`
7. `技术库/RELL样品开发执行计划.md`
8. `研发记录/2026-07-10.md`

## 第十六次追加验证方式

1. 运行 `python demo_runtime\rell_sample\validate_api_sample.py`。
2. 运行 `python demo_runtime\rell_sample\run_all_checks.py`。

## 第十六次追加验证结果

1. `validate_api_sample.py` 已通过，新增覆盖经验形成后的概念候选生成、人工确认晋升、概念库回写和晋升概念的再次解析复用。
2. `run_all_checks.py` 已通过，HTTP 烟测新增覆盖 `GET /concept/candidates`、`POST /concept/candidates/confirm` 和教学结果中的概念候选流转。
3. 当前样品已具备“统一语义入口 -> 经验形成 -> 概念候选 -> 人工确认晋升 -> 概念层再次参与解析”的最小概念生长闭环。

## 第十七次追加研发过程：概念晋升前端确认入口

1. 在 DEMO 首页新增“概念候选”与“确认晋升”两个按钮，并增加 `conceptCandidateId` 输入框，使概念晋升不再停留在接口层。
2. 教学入库、对话教学和边教边动固化经验后，页面会自动带出首个待确认概念候选标识，便于直接完成人工确认。
3. 新增前端函数 `showConceptCandidates()` 与 `confirmConceptCandidate()`，前者读取候选库并展示候选清单，后者直接调用 `POST /concept/candidates/confirm` 完成晋升。
4. `run_all_checks.py` 新增页面存在性检查，确认首页已包含概念候选与确认晋升入口，保证这条链在展示层可见。

## 第十七次追加产出文件

1. `demo_runtime/rell_sample/api_server.py`
2. `demo_runtime/rell_sample/run_all_checks.py`
3. `技术库/RELL样品开发执行计划.md`
4. `研发记录/2026-07-10.md`

## 第十七次追加验证方式

1. 运行 `python demo_runtime\rell_sample\validate_api_sample.py`。
2. 运行 `python demo_runtime\rell_sample\run_all_checks.py`。

## 第十七次追加验证结果

1. `validate_api_sample.py` 已通过，说明展示层补丁未破坏概念晋升主链。
2. `run_all_checks.py` 已通过，HTTP 烟测已确认首页包含 `conceptCandidatesButton` 和 `confirmConceptCandidateButton`。
